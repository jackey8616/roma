# Copy to `terraform.tfvars` — which is git-ignored — or pass with `-var`.
#
# Only `project_id` has no default. Everything else is here so that the names are
# visible in one place; delete any line to take the default from `variables.tf`.

project_id = "your-project-id"

# topic_name             = "roma-chat-events"
# subscription_name      = "roma-chat-events-sub"
# service_account_id     = "roma-runtime"
# ^ was `roma-agent`. An account id is immutable, so an existing deployment that
#   applies this as-is has its account, key and grants destroyed and recreated.
#   README.md's "The account was renamed" has the two ways out; the recommended
#   one leaves this commented and runs `terraform state rm` first, so that
#   `roma-agent@` survives and becomes the agent's Cloud Reach. Uncomment and pin
#   the old value only if you would rather change nothing at all.
# dead_letter_topic_name = "roma-chat-events-dead-letter"

# The three numbers worth understanding before changing — `variables.tf` says why
# each is what it is.
# ack_deadline_seconds              = 60
# message_retention_duration        = "604800s"
# dead_letter_max_delivery_attempts = 100
